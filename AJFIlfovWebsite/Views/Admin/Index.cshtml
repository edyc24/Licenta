<h2 class="futuristic-title">Admin Dashboard</h2>
<p class="welcome-message">Bine ai venit, @User.Identity.Name!</p>

<ul class="admin-menu">
    <li><a asp-action="Audit" class="futuristic-link">Inspectează Audit</a></li>
    <li><a asp-action="CreateAnunt" class="futuristic-link">Creează Anunț</a></li>
    <li><a asp-action="CreateMatch" class="futuristic-link">Creează Meci</a></li>
    <li><a asp-action="MeciuriLive" class="futuristic-link">Lista Meciuri Live</a></li>
    <li><a asp-action="Logout" class="futuristic-link">Logout</a></li>
</ul>

<style>
    /* General styles */
    body {
        background: linear-gradient(135deg, #0f0c29, #302b63, #24243e);
        margin: 0;
        padding: 0;
        display: flex;
        flex-direction: column;
        min-height: 100vh;
    }

    .futuristic-title {
        text-align: center;
        font-size: 2.5em;
        color: #00e0ff;
        margin-top: 40px;
    }

    .welcome-message {
        text-align: center;
        font-size: 1.5em;
        color: #ffffff;
        margin-bottom: 40px;
    }

    .admin-menu {
        list-style-type: none;
        padding: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center; /* Center the buttons horizontally */
        gap: 30px; /* Add a 30px gap between buttons */
    }

    .futuristic-link {
        display: block;
        width: 200px; /* Set a fixed width for all buttons */
        text-align: center;
        font-size: 1.2em;
        color: #00e0ff;
        text-decoration: none;
        padding: 10px 20px;
        border-radius: 10px;
        background-color: rgba(255, 255, 255, 0.1);
        transition: background-color 0.3s, color 0.3s;
    }

        .futuristic-link:hover {
            background-color: #ff4081;
            color: #ffffff;
        }

        .futuristic-link:hover {
            background-color: #ff4081;
            color: #ffffff;
        }

    /* Responsive styles */
    @@media (max-width: 768px) {
        .futuristic-title {
            font-size: 2em;
        }

        .welcome-message {
            font-size: 1.2em;
        }

        .futuristic-link {
            font-size: 1em;
            padding: 8px 16px;
        }
    }

    @@media (max-width: 480px) {
        .futuristic-title {
            font-size: 1.8em;
        }

        .welcome-message {
            font-size: 1em;
        }

        .futuristic-link {
            font-size: 0.9em;
            padding: 6px 12px;
        }
    }

    footer {
        background-color: transparent;
        text-align: center;
        padding: 10px 0;
        color: #ffffff;
        margin-top: auto; /* Ensures the footer is pushed to the bottom */
    }
</style>